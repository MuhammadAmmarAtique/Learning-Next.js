import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About",
  description: "Our compnay is best,we provide best services related to jobs",
};

const about = () => {
  return <div>About</div>;
};

export default about;
