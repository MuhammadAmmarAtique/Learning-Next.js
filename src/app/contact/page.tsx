import React from 'react'
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Contact",
  description: "Our compnay is best,we provide best services related to jobs, contact us now",
};

const contact = () => {
  return (
    <div>Contact</div>
  )
}

export default contact